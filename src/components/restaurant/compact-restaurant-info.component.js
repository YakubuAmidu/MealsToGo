import React from "react";
import styled from 'styled-components';

const CompactImage = styled.Image`
 border-radius: 10px;
 width: 120px;
 height: 100px;
`;

const Item = styled.View`
 padding: 10px;
 max-width: 120px;
 align-items: center;
`;

const CompactImage = styled.

export const CompactRestaurantInfo = ({ restaurant }) => {};
