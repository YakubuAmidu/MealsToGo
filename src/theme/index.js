import { colors } from "./colors";
import { space, lineHeight } from "./spacing";
import { sizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./sizes";

export const theme = {
  colors,
  space,
  lineHeight,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
};
